import type { CSSProperties } from "react";

/* ── Common label style ── ported verbatim from the design prototype
   (shell.jsx labelStyle). */
export const labelStyle: CSSProperties = { fontSize: 11, color: "var(--text-3)", textTransform: "uppercase", letterSpacing: "0.08em", fontWeight: 600 };
