# Loki config for the bundled logs stack (the `logs` compose profile).
#
# Single-binary, filesystem-backed Loki — no object store, no clustering. Sized
# for a laptop: everything lives under /loki (a named volume). A local dev log
# sink, not a production deployment.
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  # Quiet Loki's own request logging so the container output stays readable.
  log_level: warn

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

# Local dev: keep the last 7 days of logs, drop older.
limits_config:
  retention_period: 168h
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  allow_structured_metadata: true

compactor:
  working_directory: /loki/compactor
  delete_request_store: filesystem
  retention_enabled: true

analytics:
  reporting_enabled: false
